specVersion: "v1beta"
serviceId: "trakrdef"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "trakrdef-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
